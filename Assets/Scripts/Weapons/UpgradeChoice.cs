using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class UpgradeChoice : MonoBehaviour
{
    /*
    public List<soWeapon> allUp;
    public Image gunImage;
    public TextMeshProUGUI gunName;
    public TextMeshProUGUI gunDesc;

    void Start()
    {
        soWeapon randomWeapon = GetRandomWeapon();

        gunImage = randomWeapon.weaponSprite;
        gunName.text = randomWeapon.weaponName;
        gunDesc.text = randomWeapon.weaponDescription;
    }

    public soWeapon GetRandomWeapon()
    {
        return allWeapons[Random.Range(0, allWeapons.Count)];
    }
    */
}
